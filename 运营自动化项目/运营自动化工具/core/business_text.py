"""业务相关文本常量 —— 输入纠错词典 + 未知任务的示例提示。

这些是与具体业务绑定的中文文案，从通用任务解析引擎（task_registry.py）里分离出来，
便于整体替换：别人 clone 框架后改本文件即可，无需改引擎。
"""

from __future__ import annotations

# 常见输入错别字 → 正确写法（任务名归一化时统一替换）。
TEXT_CORRECTIONS: dict[str, str] = {
    "剧水潭": "聚水潭",
}

# 无法识别任务名时，追加给用户的「类似说法」示例提示。
UNKNOWN_TASK_HINT = (
    "也支持类似说法，例如：刷单登记、猫超刷单表格登记、店铺刷单登记、"
    "整理猫超对账、更新聚水潭资料。"
)
